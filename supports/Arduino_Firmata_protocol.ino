/*#This is python code belonging to this youTube video:https://www.youtube.com/watch?v=FfJ2Rk4sjH8#writen by Mos form Logmaker360 on 15/03/2016#explaning the firmata protocol#Let me know if it was useful!!!#Subscribe, leave a common or tumbs up...import sys, csvfrom matplotlib import pyplot      # dont pip matplotlib, with pip you download errors. google matplotlib and dowload it the old way..import pyfirmatafrom time import sleepimport numpy as npport = "COM4"board = pyfirmata.Arduino(port)it = pyfirmata.util.Iterator(board)it.start()potMeter = board.get_pin("a:0:i")led = board.get_pin("d:13:o")pyplot.ion()pData = [0] * 25fig = pyplot.figure()pyplot.title("Real Time Potmeter")ax1 = pyplot.axes()l1, = pyplot.plot(pData)pyplot.ylim([0,1])while True:      try:          sleep(1)          pData.append(float(potMeter.read()))          pyplot.ylim([0, 1])          del pData[0]          l1.set_xdata([i for i in xrange(25)])          pyplot.pause(0.0001)                              # make sure you pause oltherwise it will crash          l1.set_ydata(pData)          pyplot.pause(0.0001)                              # make sure you pause oltherwise it will crash          print(pData)          pyplot.draw()          if pData[24] == 1.0:              led.write(1)              print("LedOn")          else:              led.write(0)                except KeyboardInterrupt:          board.exit()          break*/void setup() {    }void loop() {    }